// write your codes here
function setup() {
    //            w    h
    createCanvas(500, 500);
    background(220);
    noLoop();
}

function draw() {
    let xpos = 50;
    let ypos = 50;
    let ggColour = 0;
    for (let num = 1; num < 6; num++); {
        fill(0, ggColour, 0);
        circle(xpos, ypos, 50);
        let xpos = xpos-50;
        let ypos = ypos-50;
        let
    }
}